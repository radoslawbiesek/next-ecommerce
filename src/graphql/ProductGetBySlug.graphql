query ProductGetBySlug($slug: String) {
  product(slug: $slug) {
    id
    name
    description
    categories {
      name
      id
    }
    price
    images {
      url
      alt
    }
    slug
  }
}
