query CategoryGetBySlug($slug: String!) {
  category(slug: $slug) {
    name
    description
    id
    products {
      data {
        ...ProductListItem
        ...ProductImages
      }
      meta {
        total
      }
    }
  }
}
