query CategoryGetBySlug($slug: String) {
  category(slug: $slug) {
    name
    description
    id
    products {
      id
      name
      slug
      description
      categories {
        name
        id
      }
      images {
        url
        alt
      }
      price
    }
  }
}
