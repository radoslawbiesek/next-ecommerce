query CollectionGetBySlug($slug: String!) {
  collection(slug: $slug) {
    name
    description
    id
    products {
      data {
        ...ProductListItem
        ...ProductImages
      }
      meta {
        total
      }
    }
  }
}
